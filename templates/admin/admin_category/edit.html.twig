{% extends 'admin.base.html.twig' %}

{% block dashboard %}
<section class="gap-40">
    <a class="btn btn-primary" href="{{ path('admin_category_index') }}">Retour à la liste</a>
    <h1>Modifier la Catégorie : {{ category.name }}</h1>

    {{ form_start(form) }}

    {{ form_widget(form) }}

    <button class="btn btn-green" type="submit">Modifier</button>

    {{ form_end(form) }}
</section>
{% endblock %}