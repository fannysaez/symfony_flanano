{% extends 'admin.base.html.twig' %}

{% block dashboard %}
<section class="gap-40">
    <a class="btn btn-primary" href="{{ path('admin_product_index') }}">Retour à la liste</a>
    <h1>Modifier le Produit : {{ product.title }}</h1>

    {{ form_start(form) }}

    {{ form_widget(form) }}

    <div class="form-img">
        <img src="{{ asset(product.cover) }}" alt="{{ product.title }}" width="100%">
    </div>

    <button class="btn btn-green" type="submit">Modifier</button>

    {{ form_end(form) }}
</section>
{% endblock %}